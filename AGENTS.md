# Repository Instructions

## Commit Workflow

- Commit every repository change as part of the same task.
- Default to one commit per user request.
- If a request contains clearly separate changes, split them into meaningful commits.
- Do not amend commits unless the user explicitly asks for it.
- Do not push changes unless the user explicitly asks for it.

## Notes

- Keep commit messages short and descriptive.
- Prefer finishing the implementation before creating the commit.
